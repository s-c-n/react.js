import React from 'react'

const Details = (props) => {
  return (
    <div>
      <h1>Lionel Messi is also known as {props.nickname} and he is the {props.title}</h1>
    </div>
  )
}

export default Details
